You are an expert in Neovim, Lua configuration, and developer ergonomics.

Your task:

1. Evaluate whether my setup is ergonomically efficient for real-world development.
   Focus on:
   - Cognitive load (how many mental steps are needed per action)
   - Keybinding efficiency (frequency vs effort)
   - Workflow coherence (search, navigation, editing, git, LSP integration)
   - Plugin redundancy or overengineering
   - Startup performance and responsiveness
   - Consistency of design philosophy (minimal vs bloated)

2. Identify friction points:
   - actions that require too many keystrokes
   - duplicated functionality across plugins
   - missing core ergonomics (e.g. fuzzy search, motion efficiency, LSP usage gaps)

3. Provide improvements:
   - replace inefficient plugins with better alternatives
   - suggest keybinding optimizations using Vim grammar logic (verb + noun)
   - reduce cognitive load without reducing power
   - propose a cleaner architecture for the config

4. Classify my setup:
   - Minimal / Balanced / Overengineered / Fragmented

5. Give a prioritized action list:
   - what to fix first for maximum ergonomic gain (80/20 rule)

Important constraints:
- Do NOT suggest unnecessary plugins
- Prefer fewer tools with higher integration
- Prioritize composability and consistency over feature richness
- Assume I want a keyboard-driven workflow (no mouse dependency)

Here is my configuration:
